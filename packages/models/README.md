# @ibus-qikai/models

`ibus-qikai` 的离线资源包。

## 包含内容

- `PP-OCRv5_rec_mobile_infer.ort`: 经过优化的移动端手写识别模型。
- `ppocrv5_dict.txt`: 对应的汉字字典文件。
- `pinyin_dict.json`: 拼音到汉字的映射字典。

## 使用

通常作为 `ibus-qikai` 聚合包的依赖被自动引用。
